#pragma once
#include <rack.hpp>
#include <logger.hpp>

using namespace rack;
using namespace std;

// Declare the Plugin, defined in plugin.cpp
extern Plugin* pluginInstance;
extern Model* modelAmenator;

// Declare each Model, defined in each module source file
// extern Model* modelMyModule;
